Stage E 第一版使用 `build_lesson_package.py` 根据 `ResearchDossier` 与 `LessonContext` 确定性生成四类 DOCX：

- `备课分析.docx`
- `教学设计.docx`
- `课堂活动单.docx`
- `配套练习.docx`

当前目录记录模板资产约定。后续若引入可编辑 `.docx` 模板，应保持文件名与 E3 输出清单一致，并继续由脚本负责来源、复核提示和教案结构的一致性校验。
